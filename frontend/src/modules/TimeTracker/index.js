import React from 'react';

const TimeTracker = () => (
    <div>TimeTracker Module</div>
);

export default {
    routeProps: {
        path: '/timetracker',
        component: TimeTracker
    },
    name: 'TimeTracker',
}
